<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>HealthScan — Detect Food Safety Instantly</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link
    href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&family=Lato:wght@300;400;700&display=swap"
    rel="stylesheet" />
  <style>
    /* ═══════════════════════════════════════════════
       TOKENS & RESET
    ═══════════════════════════════════════════════ */
    *,
    *::before,
    *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --teal: #0f766e;
      --teal-lt: #14b8a6;
      --green: #22c55e;
      --green-dk: #16a34a;
      --bg: #f8fafc;
      --bg2: #f1f5f9;
      --surface: #ffffff;
      --border: #e2e8f0;
      --text: #0f172a;
      --muted: #64748b;
      --muted-lt: #94a3b8;
      --radius-sm: 10px;
      --radius: 16px;
      --radius-lg: 24px;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      background: var(--bg);
      font-family: 'Lato', sans-serif;
      color: var(--text);
      overflow-x: hidden;
    }

    /* ═══════════════════════════════════════════════
       NAVBAR
    ═══════════════════════════════════════════════ */
    .navbar {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
      padding: 18px 0;
      transition: all 0.35s ease;
      background: transparent;
    }

    .navbar.scrolled {
      background: rgba(255, 255, 255, 0.82);
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);
      border-bottom: 1px solid var(--border);
      padding: 12px 0;
      box-shadow: 0 4px 24px rgba(15, 118, 110, 0.08);
    }

    .navbar-brand {
      font-family: 'Nunito', sans-serif;
      font-weight: 800;
      font-size: 1.35rem;
      color: var(--teal) !important;
      letter-spacing: -0.5px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .brand-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--green);
      display: inline-block;
      animation: pulse-dot 2s ease-in-out infinite;
    }

    @keyframes pulse-dot {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.5);
      }

      50% {
        box-shadow: 0 0 0 6px rgba(34, 197, 94, 0);
      }
    }

    .nav-link {
      font-size: 0.9rem;
      font-weight: 500;
      color: var(--text) !important;
      padding: 6px 4px !important;
      margin: 0 12px;
      position: relative;
      transition: color 0.25s;
    }

    .nav-link::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 0;
      height: 2px;
      background: var(--teal-lt);
      border-radius: 2px;
      transition: width 0.3s ease;
    }

    .nav-link:hover {
      color: var(--teal) !important;
    }

    .nav-link:hover::after {
      width: 100%;
    }

    .btn-nav-cta {
      background: var(--teal);
      color: #fff !important;
      border-radius: 50px;
      padding: 9px 22px !important;
      font-size: 0.875rem !important;
      font-weight: 600 !important;
      margin-left: 8px !important;
      transition: all 0.3s ease;
      box-shadow: 0 0 0 0 rgba(15, 118, 110, 0.4);
    }

    .navbar-toggler {
      border: 1.5px solid rgba(15, 118, 110, 0.3) !important;
      border-radius: 8px;
      padding: 6px 10px;
    }

    .navbar-toggler:focus {
      box-shadow: none;
    }

    .navbar-toggler-icon {
      background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='%230f766e' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e") !important;
    }

    .btn-nav-cta::after {
      display: none;
    }

    .btn-nav-cta:hover {
      background: var(--teal-lt);
      transform: translateY(-1px);
      box-shadow: 0 6px 20px rgba(15, 118, 110, 0.35);
    }

    /* ═══════════════════════════════════════════════
       HERO
    ═══════════════════════════════════════════════ */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 120px 0 80px;
      position: relative;
      overflow: hidden;
      background: var(--bg);
    }

    /* Background blobs */
    .blob {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      opacity: 0.35;
      pointer-events: none;
    }

    .blob-1 {
      width: 520px;
      height: 520px;
      background: radial-gradient(circle, #99f6e4, #0d9488);
      top: -120px;
      right: -80px;
      animation: drift1 14s ease-in-out infinite;
    }

    .blob-2 {
      width: 360px;
      height: 360px;
      background: radial-gradient(circle, #bbf7d0, #22c55e);
      bottom: 0;
      left: -100px;
      animation: drift2 18s ease-in-out infinite;
    }

    .blob-3 {
      width: 200px;
      height: 200px;
      background: radial-gradient(circle, #e0f2fe, #38bdf8);
      top: 40%;
      left: 42%;
      animation: drift3 12s ease-in-out infinite;
    }

    @keyframes drift1 {

      0%,
      100% {
        transform: translate(0, 0) scale(1);
      }

      50% {
        transform: translate(30px, 40px) scale(1.05);
      }
    }

    @keyframes drift2 {

      0%,
      100% {
        transform: translate(0, 0) scale(1);
      }

      50% {
        transform: translate(-20px, -30px) scale(1.08);
      }
    }

    @keyframes drift3 {

      0%,
      100% {
        transform: translate(0, 0);
      }

      50% {
        transform: translate(20px, -20px);
      }
    }

    /* Pill badge */
    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      background: rgba(20, 184, 166, 0.1);
      border: 1px solid rgba(20, 184, 166, 0.3);
      color: var(--teal);
      font-size: 0.78rem;
      font-weight: 600;
      padding: 6px 14px;
      border-radius: 50px;
      letter-spacing: 0.5px;
      text-transform: uppercase;
      margin-bottom: 24px;
      opacity: 0;
      animation: slideUp 0.7s ease forwards 0.2s;
    }

    .badge-dot {
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--green);
    }

    /* Hero text */
    .hero-title {
      font-family: 'Nunito', sans-serif;
      font-size: clamp(2rem, 3.2vw, 3rem);
      font-weight: 900;
      line-height: 1.18;
      letter-spacing: -0.5px;
      color: var(--text);
      opacity: 0;
      animation: slideUp 0.75s ease forwards 0.35s;
    }

    .hero-title .highlight {
      background: linear-gradient(135deg, var(--teal), var(--teal-lt), var(--green));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-sub {
      font-size: 1.05rem;
      color: var(--muted);
      line-height: 1.75;
      max-width: 480px;
      margin-top: 18px;
      font-weight: 400;
      opacity: 0;
      animation: slideUp 0.75s ease forwards 0.5s;
    }

    .hero-actions {
      display: flex;
      align-items: center;
      gap: 14px;
      margin-top: 36px;
      flex-wrap: wrap;
      opacity: 0;
      animation: slideUp 0.75s ease forwards 0.65s;
    }

    .btn-primary-cta {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--teal);
      color: #fff;
      border: none;
      border-radius: 50px;
      padding: 14px 30px;
      font-family: 'Lato', sans-serif;
      font-size: 0.95rem;
      font-weight: 700;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 4px 20px rgba(15, 118, 110, 0.3);
    }

    .btn-primary-cta:hover {
      background: #0d6b63;
      transform: translateY(-3px);
      box-shadow: 0 10px 32px rgba(15, 118, 110, 0.45);
      color: #fff;
    }

    .btn-primary-cta .arrow {
      transition: transform 0.25s ease;
    }

    .btn-primary-cta:hover .arrow {
      transform: translateX(4px);
    }

    .btn-outline-cta {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: transparent;
      color: var(--text);
      border: 1.5px solid var(--border);
      border-radius: 50px;
      padding: 13px 26px;
      font-family: 'Lato', sans-serif;
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .btn-outline-cta:hover {
      border-color: var(--teal-lt);
      color: var(--teal);
      background: rgba(20, 184, 166, 0.05);
      transform: translateY(-2px);
    }

    /* Trust row */
    .trust-row {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-top: 48px;
      opacity: 0;
      animation: slideUp 0.75s ease forwards 0.8s;
    }

    .trust-divider {
      width: 1px;
      height: 28px;
      background: var(--border);
    }

    .trust-item {
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .trust-number {
      font-family: 'Nunito', sans-serif;
      font-size: 1.25rem;
      font-weight: 800;
      color: var(--teal);
    }

    .trust-label {
      font-size: 0.73rem;
      color: var(--muted-lt);
      font-weight: 500;
      letter-spacing: 0.3px;
    }

    /* Hero image panel */
    .hero-visual {
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      animation: fadeIn 1s ease forwards 0.5s;
    }

    .hero-img-wrap {
      position: relative;
      width: 100%;
      max-width: 440px;
    }

    .hero-img-bg {
      position: absolute;
      inset: -20px;
      background: radial-gradient(ellipse at center, rgba(20, 184, 166, 0.15) 0%, transparent 70%);
      border-radius: 50%;
    }

    .hero-img {
      width: 100%;
      height: auto;
      border-radius: var(--radius-lg);
      position: relative;
      z-index: 1;
      animation: float 5s ease-in-out infinite;
      filter: drop-shadow(0 24px 48px rgba(15, 118, 110, 0.2));
    }

    /* Floating cards on hero image */
    .float-card {
      position: absolute;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 12px 16px;
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 0.82rem;
      font-weight: 600;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
      z-index: 2;
      white-space: nowrap;
    }

    .float-card-icon {
      width: 32px;
      height: 32px;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1rem;
    }

    .float-card-1 {
      top: 10%;
      left: -60px;
      animation: float 6s ease-in-out infinite 0.5s;
    }

    .float-card-2 {
      bottom: 15%;
      right: -50px;
      animation: float 5.5s ease-in-out infinite 1s;
    }

    .fc-green {
      background: rgba(34, 197, 94, 0.12);
    }

    .fc-teal {
      background: rgba(20, 184, 166, 0.12);
    }

    .float-text {
      color: var(--text);
    }

    .float-sub {
      font-size: 0.7rem;
      color: var(--muted);
      font-weight: 400;
    }

    @keyframes float {

      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-12px);
      }
    }

    /* ═══════════════════════════════════════════════
       SECTION SHARED
    ═══════════════════════════════════════════════ */
    .section-eyebrow {
      font-size: 0.75rem;
      letter-spacing: 2.5px;
      text-transform: uppercase;
      font-weight: 700;
      color: var(--teal);
      margin-bottom: 12px;
    }

    .section-title {
      font-family: 'Nunito', sans-serif;
      font-size: clamp(1.6rem, 2.4vw, 2.2rem);
      font-weight: 800;
      letter-spacing: -0.3px;
      line-height: 1.25;
      color: var(--text);
    }

    .section-sub {
      color: var(--muted);
      font-size: 1rem;
      line-height: 1.7;
      max-width: 520px;
      margin: 0 auto;
    }

    /* ═══════════════════════════════════════════════
       INFO / CARDS SECTION
    ═══════════════════════════════════════════════ */
    .info-section {
      padding: 100px 0;
      background: var(--bg);
    }

    .info-header {
      text-align: center;
      margin-bottom: 64px;
    }

    /* Feature card */
    .feat-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius-lg);
      padding: 36px 32px;
      height: 100%;
      transition: all 0.4s ease;
      position: relative;
      overflow: hidden;
      opacity: 0;
      transform: translateY(32px);
    }

    .feat-card.visible {
      opacity: 1;
      transform: translateY(0);
      transition: opacity 0.55s ease, transform 0.55s ease;
    }

    .feat-card:hover {
      border-color: rgba(20, 184, 166, 0.4);
      box-shadow: 0 20px 56px rgba(15, 118, 110, 0.12);
      transform: translateY(-6px) !important;
    }

    /* Hover shimmer line */
    .feat-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--teal), var(--green));
      opacity: 0;
      transition: opacity 0.3s ease;
      border-radius: var(--radius-lg) var(--radius-lg) 0 0;
    }

    .feat-card:hover::before {
      opacity: 1;
    }

    .feat-icon-wrap {
      width: 52px;
      height: 52px;
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.4rem;
      margin-bottom: 22px;
    }

    .ic-teal {
      background: rgba(20, 184, 166, 0.1);
    }

    .ic-green {
      background: rgba(34, 197, 94, 0.1);
    }

    .ic-blue {
      background: rgba(56, 189, 248, 0.1);
    }

    .feat-title {
      font-family: 'Nunito', sans-serif;
      font-size: 1.15rem;
      font-weight: 700;
      color: var(--text);
      margin-bottom: 10px;
    }

    .feat-text {
      font-size: 0.9rem;
      color: var(--muted);
      line-height: 1.7;
    }

    .feat-link {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      color: var(--teal);
      font-size: 0.85rem;
      font-weight: 600;
      text-decoration: none;
      margin-top: 20px;
      transition: gap 0.25s ease;
    }

    .feat-link:hover {
      gap: 10px;
      color: var(--teal-lt);
    }

    /* ═══════════════════════════════════════════════
       HOW IT WORKS
    ═══════════════════════════════════════════════ */
    .how-section {
      padding: 100px 0;
      background: var(--bg2);
    }

    .steps-wrap {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2px;
      position: relative;
      margin-top: 60px;
    }

    .steps-wrap::before {
      content: '';
      position: absolute;
      top: 28px;
      left: calc(16.6% + 28px);
      right: calc(16.6% + 28px);
      height: 1px;
      background: linear-gradient(90deg, var(--teal), var(--green));
      opacity: 0.3;
    }

    .step-item {
      text-align: center;
      padding: 0 24px;
      opacity: 0;
      transform: translateY(24px);
    }

    .step-item.visible {
      opacity: 1;
      transform: translateY(0);
      transition: opacity 0.5s ease, transform 0.5s ease;
    }

    .step-num {
      width: 56px;
      height: 56px;
      border-radius: 50%;
      background: var(--surface);
      border: 2px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
      font-family: 'Nunito', sans-serif;
      font-size: 1.1rem;
      font-weight: 800;
      color: var(--teal);
      position: relative;
      z-index: 1;
      transition: all 0.3s ease;
    }

    .step-item:hover .step-num {
      background: var(--teal);
      color: #fff;
      border-color: var(--teal);
      box-shadow: 0 0 0 6px rgba(15, 118, 110, 0.15);
    }

    .step-title {
      font-family: 'Nunito', sans-serif;
      font-weight: 700;
      font-size: 1rem;
      margin-bottom: 8px;
      color: var(--text);
    }

    .step-desc {
      font-size: 0.87rem;
      color: var(--muted);
      line-height: 1.65;
    }

    /* ═══════════════════════════════════════════════
       CTA BANNER WITH SCANNING ANIMATION
    ═══════════════════════════════════════════════ */
    .cta-section {
      padding: 80px 0;
      background: var(--bg);
    }

    .cta-inner {
      background: linear-gradient(135deg, var(--teal) 0%, #0d9488 50%, #0f766e 100%);
      border-radius: var(--radius-lg);
      padding: 64px 56px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 40px;
      position: relative;
      overflow: hidden;
    }

    .cta-inner::before {
      content: '';
      position: absolute;
      top: -60px;
      right: -60px;
      width: 300px;
      height: 300px;
      background: rgba(255, 255, 255, 0.07);
      border-radius: 50%;
    }

    .cta-inner::after {
      content: '';
      position: absolute;
      bottom: -80px;
      left: 30%;
      width: 220px;
      height: 220px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 50%;
    }

    /* Scanning waves animation */
    .scan-waves {
      position: absolute;
      inset: 0;
      pointer-events: none;
      z-index: 0;
    }

    .wave-line {
      position: absolute;
      left: -100%;
      width: 100%;
      height: 2px;
      background: linear-gradient(90deg,
          transparent 0%,
          rgba(255, 255, 255, 0.4) 20%,
          rgba(255, 255, 255, 0.8) 50%,
          rgba(255, 255, 255, 0.4) 80%,
          transparent 100%);
      animation: scanWave 4s linear infinite;
      filter: blur(1px);
    }

    .wave-line:nth-child(1) {
      top: 25%;
      animation-delay: 0s;
    }

    .wave-line:nth-child(2) {
      top: 50%;
      animation-delay: 1.3s;
      opacity: 0.7;
    }

    .wave-line:nth-child(3) {
      top: 75%;
      animation-delay: 2.6s;
      opacity: 0.5;
    }

    @keyframes scanWave {
      0% {
        left: -100%;
        opacity: 0;
      }

      10% {
        opacity: 1;
      }

      90% {
        opacity: 1;
      }

      100% {
        left: 100%;
        opacity: 0;
      }
    }

    /* Floating scan particles */
    .scan-particles {
      position: absolute;
      inset: 0;
      pointer-events: none;
      z-index: 0;
    }

    .particle {
      position: absolute;
      width: 4px;
      height: 4px;
      background: rgba(255, 255, 255, 0.6);
      border-radius: 50%;
      box-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
    }

    .particle:nth-child(1) {
      top: 20%;
      left: 10%;
      animation: floatParticle 6s ease-in-out infinite;
    }

    .particle:nth-child(2) {
      top: 60%;
      left: 30%;
      animation: floatParticle 7s ease-in-out infinite 1s;
    }

    .particle:nth-child(3) {
      top: 40%;
      left: 70%;
      animation: floatParticle 8s ease-in-out infinite 2s;
    }

    .particle:nth-child(4) {
      top: 80%;
      left: 50%;
      animation: floatParticle 6.5s ease-in-out infinite 1.5s;
    }

    .particle:nth-child(5) {
      top: 30%;
      left: 85%;
      animation: floatParticle 7.5s ease-in-out infinite 0.5s;
    }

    @keyframes floatParticle {

      0%,
      100% {
        transform: translate(0, 0) scale(1);
        opacity: 0;
      }

      10% {
        opacity: 1;
      }

      50% {
        transform: translate(30px, -20px) scale(1.2);
        opacity: 0.8;
      }

      90% {
        opacity: 1;
      }
    }

    /* Wavy gradient overlay */
    .wave-gradient {
      position: absolute;
      inset: 0;
      background: linear-gradient(45deg,
          rgba(255, 255, 255, 0) 0%,
          rgba(255, 255, 255, 0.05) 50%,
          rgba(255, 255, 255, 0) 100%);
      background-size: 200% 200%;
      animation: waveGradient 8s ease-in-out infinite;
      pointer-events: none;
      z-index: 0;
    }

    @keyframes waveGradient {

      0%,
      100% {
        background-position: 0% 50%;
      }

      50% {
        background-position: 100% 50%;
      }
    }

    /* Pulsing glow effect */
    .pulse-glow {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px;
      height: 300px;
      background: radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
      border-radius: 50%;
      animation: pulseGlow 3s ease-in-out infinite;
      pointer-events: none;
      z-index: 0;
    }

    @keyframes pulseGlow {

      0%,
      100% {
        transform: translate(-50%, -50%) scale(1);
        opacity: 0.3;
      }

      50% {
        transform: translate(-50%, -50%) scale(1.2);
        opacity: 0.6;
      }
    }

    /* Ensure text content stays above animations */
    .cta-text,
    .btn-cta-white {
      position: relative;
      z-index: 1;
    }

    .cta-text .cta-label {
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.65);
      margin-bottom: 12px;
    }

    .cta-text h2 {
      font-family: 'Nunito', sans-serif;
      font-size: 1.75rem;
      font-weight: 800;
      color: #fff;
      letter-spacing: -0.3px;
      line-height: 1.3;
    }

    .cta-text p {
      color: rgba(255, 255, 255, 0.7);
      margin-top: 10px;
      font-size: 0.95rem;
    }

    .btn-cta-white {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: #fff;
      color: var(--teal);
      border: none;
      border-radius: 50px;
      padding: 15px 32px;
      font-family: 'Lato', sans-serif;
      font-size: 0.95rem;
      font-weight: 700;
      cursor: pointer;
      text-decoration: none;
      white-space: nowrap;
      transition: all 0.3s ease;
      flex-shrink: 0;
    }

    .btn-cta-white:hover {
      background: #f0fdf4;
      transform: translateY(-3px);
      box-shadow: 0 10px 28px rgba(0, 0, 0, 0.2);
      color: var(--teal);
    }

    /* ═══════════════════════════════════════════════
       FOOTER
    ═══════════════════════════════════════════════ */
    footer {
      background: #0a0f0e;
      color: rgba(255, 255, 255, 0.55);
      padding: 56px 0 28px;
    }

    .footer-brand {
      font-family: 'Nunito', sans-serif;
      font-weight: 800;
      font-size: 1.2rem;
      color: #fff;
      display: flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
      margin-bottom: 12px;
    }

    .footer-tagline {
      font-size: 0.85rem;
      line-height: 1.6;
      max-width: 240px;
    }

    .footer-col-title {
      font-size: 0.8rem;
      font-weight: 700;
      color: rgba(255, 255, 255, 0.8);
      letter-spacing: 1px;
      text-transform: uppercase;
      margin-bottom: 16px;
    }

    .footer-links {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .footer-links a {
      color: rgba(255, 255, 255, 0.5);
      text-decoration: none;
      font-size: 0.87rem;
      transition: color 0.2s;
    }

    .footer-links a:hover {
      color: var(--teal-lt);
    }

    .footer-divider {
      border: none;
      border-top: 1px solid rgba(255, 255, 255, 0.08);
      margin: 40px 0 24px;
    }

    .footer-bottom {
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: 0.8rem;
      flex-wrap: wrap;
      gap: 10px;
    }

    .footer-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      background: rgba(255, 255, 255, 0.06);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 50px;
      padding: 5px 12px;
      font-size: 0.75rem;
      color: rgba(255, 255, 255, 0.45);
    }

    /* ═══════════════════════════════════════════════
       KEYFRAMES & SCROLL REVEAL
    ═══════════════════════════════════════════════ */
    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(24px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* ═══════════════════════════════════════════════
       RESPONSIVE
    ═══════════════════════════════════════════════ */
    /* ═══════════════════════════════════════════════
       RESPONSIVE — TABLET  (≤ 991px)
    ═══════════════════════════════════════════════ */
    @media (max-width: 991px) {

      /* Navbar */
      .navbar {
        background: rgba(255, 255, 255, 0.95) !important;
        backdrop-filter: blur(18px);
        border-bottom: 1px solid var(--border);
        padding: 14px 0;
      }

      .navbar-collapse {
        background: #fff;
        border-radius: var(--radius);
        padding: 16px 20px;
        margin-top: 10px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        border: 1px solid var(--border);
      }

      .nav-link {
        margin: 4px 0;
        padding: 10px 8px !important;
      }

      .btn-nav-cta {
        display: inline-block;
        margin: 10px 0 4px !important;
        text-align: center;
      }

      /* Hero */
      .hero {
        padding: 100px 0 64px;
        min-height: auto;
      }

      .hero .row {
        flex-direction: column-reverse;
        gap: 0;
      }

      .hero-visual {
        margin-top: 0;
        margin-bottom: 40px;
      }

      .hero-img-wrap {
        max-width: 300px;
        margin: 0 auto;
      }

      .float-card-1 {
        top: 0;
        left: 0;
        transform: scale(0.85);
      }

      .float-card-2 {
        bottom: 0;
        right: 0;
        transform: scale(0.85);
      }

      .hero-title {
        font-size: clamp(2rem, 5vw, 2.8rem);
      }

      .hero-sub {
        max-width: 100%;
      }

      /* Steps */
      .steps-wrap {
        grid-template-columns: 1fr;
        gap: 32px;
        max-width: 420px;
        margin: 48px auto 0;
      }

      .steps-wrap::before {
        display: none;
      }

      .step-item {
        text-align: left;
        display: flex;
        align-items: flex-start;
        gap: 20px;
        padding: 0;
      }

      .step-num {
        flex-shrink: 0;
        margin: 0;
      }

      /* CTA */
      .cta-inner {
        flex-direction: column;
        text-align: center;
        padding: 48px 32px;
        gap: 28px;
      }

      /* Info section */
      .info-section {
        padding: 72px 0;
      }

      .how-section {
        padding: 72px 0;
      }

      .cta-section {
        padding: 56px 0;
      }

      /* Footer */
      .footer-bottom {
        flex-direction: column;
        text-align: center;
        gap: 12px;
      }
    }

    /* ═══════════════════════════════════════════════
       RESPONSIVE — MOBILE  (≤ 640px)
    ═══════════════════════════════════════════════ */
    @media (max-width: 640px) {

      /* Navbar */
      .navbar-brand {
        font-size: 1.15rem;
      }

      /* Hero */
      .hero {
        padding: 88px 0 56px;
      }

      .hero-title {
        font-size: clamp(1.75rem, 7vw, 2.2rem);
        letter-spacing: -0.5px;
      }

      .hero-badge {
        font-size: 0.72rem;
        padding: 5px 12px;
      }

      .hero-sub {
        font-size: 0.95rem;
        margin-top: 14px;
      }

      .hero-actions {
        flex-direction: column;
        align-items: stretch;
        margin-top: 28px;
        gap: 10px;
      }

      .btn-primary-cta,
      .btn-outline-cta {
        justify-content: center;
        padding: 13px 24px;
        font-size: 0.9rem;
      }

      .float-card-1,
      .float-card-2 {
        display: none;
      }

      .hero-img-wrap {
        max-width: 220px;
      }

      /* Trust row */
      .trust-row {
        margin-top: 32px;
        gap: 16px 24px;
        flex-wrap: wrap;
      }

      .trust-divider {
        display: none;
      }

      .trust-number {
        font-size: 1.1rem;
      }

      .trust-label {
        font-size: 0.7rem;
      }

      /* Cards */
      .feat-card {
        padding: 28px 24px;
      }

      .feat-icon-wrap {
        width: 44px;
        height: 44px;
        font-size: 1.2rem;
        margin-bottom: 16px;
      }

      .feat-title {
        font-size: 1.05rem;
      }

      /* Steps */
      .step-item {
        gap: 16px;
      }

      .step-num {
        width: 46px;
        height: 46px;
        font-size: 1rem;
      }

      /* CTA */
      .cta-inner {
        padding: 40px 24px;
        border-radius: var(--radius);
      }

      .cta-text h2 {
        font-size: 1.4rem;
      }

      .btn-cta-white {
        width: 100%;
        justify-content: center;
        padding: 14px 20px;
      }

      /* Info & How sections */
      .info-section {
        padding: 60px 0;
      }

      .how-section {
        padding: 60px 0;
      }

      .cta-section {
        padding: 40px 0;
      }

      .info-header {
        margin-bottom: 40px;
      }

      .section-title {
        font-size: 1.5rem;
      }

      .section-sub {
        font-size: 0.9rem;
      }

      /* Footer */
      footer {
        padding: 44px 0 24px;
      }

      .footer-brand {
        font-size: 1.1rem;
      }

      .footer-tagline {
        font-size: 0.82rem;
        max-width: 100%;
      }

      .footer-col-title {
        margin-top: 8px;
      }

      .footer-links a {
        font-size: 0.84rem;
      }

      .footer-bottom {
        font-size: 0.78rem;
      }

      .footer-badge {
        font-size: 0.72rem;
      }
    }

    /* ═══════════════════════════════════════════════
       RESPONSIVE — SMALL MOBILE  (≤ 400px)
    ═══════════════════════════════════════════════ */
    @media (max-width: 400px) {
      .hero-title {
        font-size: 1.6rem;
      }

      .hero-img-wrap {
        max-width: 180px;
      }

      .cta-text h2 {
        font-size: 1.25rem;
      }

      .feat-card {
        padding: 22px 18px;
      }
    }
  </style>
</head>

<body>

  <!-- ═══ NAVBAR ═══ -->
  <nav class="navbar navbar-expand-lg" id="mainNav">
    <div class="container">
      <a class="navbar-brand" href="/index">
        <span class="brand-dot"></span>
        HealthScan
      </a>
      <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navContent">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navContent">
        <ul class="navbar-nav ms-auto align-items-lg-center">
          <li class="nav-item"><a class="nav-link" href="/index">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="#info">Features</a></li>
          <li class="nav-item"><a class="nav-link" href="#how">How It Works</a></li>
          <li class="nav-item">
            <a class="nav-link btn-nav-cta" href="/scan">Test Your Product →</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- ═══ HERO ═══ -->
  <section class="hero">
    <div class="blob blob-1"></div>
    <div class="blob blob-2"></div>
    <div class="blob blob-3"></div>

    <div class="container">
      <div class="row align-items-center">

        <!-- Text column -->
        <div class="col-lg-6 order-2 order-lg-1">
          <div class="hero-badge">
            <span class="badge-dot"></span>
            AI-Powered Health Detection
          </div>
          <h1 class="hero-title">
            Know If Your Food Is<br>
            <span class="highlight">Actually Healthy</span>
          </h1>
          <p class="hero-sub">
            Instantly scan any food product's nutritional label and get a
            dual-model AI verdict tailored to your personal health profile.
          </p>
          <div class="hero-actions">
            <a href="/scan" class="btn-primary-cta">
              Scan a Product <span class="arrow">→</span>
            </a>
            <a href="#info" class="btn-outline-cta">
              See How It Works
            </a>
          </div>
          <div class="trust-row">
            <div class="trust-item">
              <span class="trust-number">2</span>
              <span class="trust-label">AI Models</span>
            </div>
            <div class="trust-divider"></div>
            <div class="trust-item">
              <span class="trust-number">8+</span>
              <span class="trust-label">Nutrients Analysed</span>
            </div>
            <div class="trust-divider"></div>
            <div class="trust-item">
              <span class="trust-number">3</span>
              <span class="trust-label">Conditions Detected</span>
            </div>
          </div>
        </div>

        <!-- Visual column -->
        <div class="col-lg-6 order-1 order-lg-2">
          <div class="hero-visual">
            <div class="hero-img-wrap">
              <div class="hero-img-bg"></div>
              <img
                src="https://png.pngtree.com/png-clipart/20230904/original/pngtree-100-natural-logos-vector-png-image_10831687.png"
                alt="HealthScan" class="hero-img" />
            </div>

            <!-- Floating micro-cards -->
            <div class="float-card float-card-1">
              <div class="float-card-icon fc-green">✅</div>
              <div>
                <div class="float-text">XGBoost Result</div>
                <div class="float-sub">Healthy — 94.2%</div>
              </div>
            </div>

            <div class="float-card float-card-2">
              <div class="float-card-icon fc-teal">🧠</div>
              <div>
                <div class="float-text">Logistic Model</div>
                <div class="float-sub">Analysing label…</div>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ═══ FEATURES ═══ -->
  <section id="info" class="info-section">
    <div class="container">
      <div class="info-header">
        <p class="section-eyebrow">Why HealthScan</p>
        <h2 class="section-title">Built for Smarter Nutrition Decisions</h2>
        <p class="section-sub mt-3">
          Combining clinical nutritional science with machine learning so you always
          know what goes into your body.
        </p>
      </div>

      <div class="row g-4">
        <div class="col-md-4">
          <div class="feat-card scroll-reveal" data-delay="0">
            <div class="feat-icon-wrap ic-teal">🩺</div>
            <div class="feat-title">Condition-Aware Scoring</div>
            <p class="feat-text">
              Enter your health profile — diabetes, obesity, hypertension — and
              get a score calibrated specifically for your body's needs.
            </p>
            <a href="/form" class="feat-link">Get your score →</a>
          </div>
        </div>
        <div class="col-md-4">
          <div class="feat-card scroll-reveal" data-delay="120">
            <div class="feat-icon-wrap ic-green">⚡</div>
            <div class="feat-title">Dual-Model Confidence</div>
            <p class="feat-text">
              Two independent AI models — Logistic Regression and XGBoost —
              cross-validate every prediction so you always get a reliable answer.
            </p>
            <a href="/form" class="feat-link">Try a scan →</a>
          </div>
        </div>
        <div class="col-md-4">
          <div class="feat-card scroll-reveal" data-delay="240">
            <div class="feat-icon-wrap ic-blue">📊</div>
            <div class="feat-title">8-Nutrient Deep Analysis</div>
            <p class="feat-text">
              Calories, protein, carbs, fat, fiber, sugars, sodium, and cholesterol
              are all weighted together for a holistic health verdict.
            </p>
            <a href="/form" class="feat-link">See what we check →</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ HOW IT WORKS ═══ -->
  <section id="how" class="how-section">
    <div class="container">
      <div class="text-center">
        <p class="section-eyebrow">The Process</p>
        <h2 class="section-title">Three Steps to Clarity</h2>
      </div>

      <div class="steps-wrap">
        <div class="step-item scroll-reveal" data-delay="0">
          <div class="step-num">01</div>
          <div>
            <h3 class="step-title">Enter Nutrition Data</h3>
            <p class="step-desc">Type in the values directly from the back of any food product package.</p>
          </div>
        </div>
        <div class="step-item scroll-reveal" data-delay="150">
          <div class="step-num">02</div>
          <div>
            <h3 class="step-title">Set Your Profile</h3>
            <p class="step-desc">Tell us about diabetes, obesity, or blood pressure conditions so results are
              personalised.</p>
          </div>
        </div>
        <div class="step-item scroll-reveal" data-delay="300">
          <div class="step-num">03</div>
          <div>
            <h3 class="step-title">Get Your Verdict</h3>
            <p class="step-desc">Two AI models independently classify the product and display confidence scores
              instantly.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ CTA BANNER ═══ -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-inner scroll-reveal" data-delay="0">

        <!-- Scanning wave animations -->
        <div class="scan-waves">
          <div class="wave-line"></div>
          <div class="wave-line"></div>
          <div class="wave-line"></div>
        </div>

        <!-- Floating particles -->
        <div class="scan-particles">
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
        </div>

        <!-- Wavy gradient overlay -->
        <div class="wave-gradient"></div>

        <!-- Pulsing glow -->
        <div class="pulse-glow"></div>

        <div class="cta-text">
          <p class="cta-label">Ready to start?</p>
          <h2>Scan your first product<br>in under 60 seconds.</h2>
          <p>No account needed. Just the nutrition label.</p>
        </div>
        <a href="/scan" class="btn-cta-white">
          Start Scanning →
        </a>
      </div>
    </div>
  </section>

  <!-- ═══ FOOTER ═══ -->
  <footer>
    <div class="container">
      <div class="row g-5">
        <div class="col-lg-4">
          <a href="/" class="footer-brand">
            <span style="width:8px;height:8px;border-radius:50%;background:var(--green);display:inline-block;"></span>
            HealthScan
          </a>
          <p class="footer-tagline">
            AI-powered food health detection tailored to your personal health conditions.
          </p>
        </div>
        <div class="col-6 col-lg-2">
          <p class="footer-col-title">Product</p>
          <ul class="footer-links">
            <li><a href="/">Home</a></li>
            <li><a href="/scan">Scan Product</a></li>
            <li><a href="#info">Features</a></li>
          </ul>
        </div>
        <div class="col-6 col-lg-2">
          <p class="footer-col-title">Health</p>
          <ul class="footer-links">
            <li><a href="#">Diabetes Guide</a></li>
            <li><a href="#">Obesity Info</a></li>
            <li><a href="#">BP Resources</a></li>
          </ul>
        </div>
        <div class="col-6 col-lg-2">
          <p class="footer-col-title">Company</p>
          <ul class="footer-links">
            <li><a href="#">About</a></li>
            <li><a href="#">Privacy Policy</a></li>
            <li><a href="#">Terms of Use</a></li>
          </ul>
        </div>
      </div>

      <hr class="footer-divider" />

      <div class="footer-bottom">
        <span>© 2025 HealthScan. All rights reserved.</span>
        <span class="footer-badge">
          <span style="width:6px;height:6px;border-radius:50%;background:#22c55e;"></span>
          All systems operational
        </span>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    /* ── Navbar scroll effect ── */
    const nav = document.getElementById('mainNav');
    window.addEventListener('scroll', () => {
      nav.classList.toggle('scrolled', window.scrollY > 40);
    });

    /* ── Scroll reveal via Intersection Observer ── */
    const reveals = document.querySelectorAll('.scroll-reveal');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const delay = entry.target.dataset.delay || 0;
          setTimeout(() => entry.target.classList.add('visible'), Number(delay));
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15 });

    reveals.forEach(el => observer.observe(el));
  </script>
</body>

</html>